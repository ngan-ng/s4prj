import { Typography } from '@mui/material';
import { Fragment } from 'react';

const Header = () => {
  return (
    <Fragment>
      <Typography>Client Header here</Typography>
    </Fragment>
  );
};

export default Header;
